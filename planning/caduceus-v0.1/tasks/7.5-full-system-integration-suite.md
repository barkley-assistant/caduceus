---
task_id: "7.5"
title: "Full-system integration suite"
execution_phase: 7
phase_file: "../phases/07-orchestration.md"
depends_on: ["7.1", "7.2", "7.3", "7.4"]
owns: "fixture helpers only"
primary_tests: "`tests/integration_test.rs`"
---

# Task 7.5: Full-system integration suite

## Outcome and required behavior
Build reusable fixtures: Wiremock GitHub server with request expectations, local main repo plus bare origin and `origin/HEAD`, executable worker scripts, isolated config/state, and the real binary where CLI behavior matters.

Required scenarios:

1. Code success: repository discovery, paginated issue list, verify, detail/comments/timeline, prompt/env, source edit, result, commit, remote branch, one PR, one marker comment, close, done state, removed worktree/claim, persisted transcript/metadata.
2. Investigation success: findings comment and label removal, no commit/push/PR/close.
3. Second idle invocation: persisted ETags cause 304 and status reports `Idle304`.
4. Partial PR response failure followed by retry: one remote branch and one PR.
5. Timeout with grandchild: both processes die, transcript persists, issue requeues/then fails at exact budget.
6. Two concurrent binaries: only one makes HTTP calls and runs a worker.
7. Rate limit on page two: observation persists, exit 0, next pre-reset tick makes zero calls.
8. Corrupt `state.json`: exit 1, original bytes preserved, no worker/API mutation.
9. Dry-run: worker and validation occur, report persists, no git/GitHub mutation, worktree removed.
10. SIGTERM: process tree dies and claim returns to queued without attempt increment.

Every Wiremock mutation has an exact expected call count. Tests assert both desired effects and forbidden effects.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/7.5.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
