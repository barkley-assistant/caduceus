---
task_id: "1.1"
title: "Parse and validate Config"
execution_phase: 1
phase_file: "../phases/01-foundations.md"
depends_on: ["0.1"]
owns: "`src/config.rs`"
primary_tests: "`tests/config_test.rs`"
---

# Task 1.1: Parse and validate Config

## Outcome and required behavior
Implement the canonical `Config` contract and a private deserialization layer so missing `worker_command` can be resolved after the source path is known. Compile regex patterns during validation and reject malformed allowlist IDs, invalid repository slugs, zero durations/budgets, duplicate trigger labels, and forbidden attempts to allow GitHub credential variables.

Tests: minimal plugin-derived config; every default; explicit replacement semantics for all lists; leading-tilde expansion; exact `${plugin_root}` worker-argument expansion; rejection of unknown interpolation; empty/zero rejection; invalid regex; default case-sensitive ignore matching and explicit `(?i)` opt-in; malformed `id:`; duplicate labels; unknown YAML field rejection using `deny_unknown_fields`; standalone missing-worker error; secure directory/file modes; symlinked state path; wrong-owner path; and unsafe existing permissions.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/1.1.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
