---
task_id: "7.0"
title: "Define orchestration-owned types and dependency injection"
execution_phase: 7
phase_file: "../phases/07-orchestration.md"
depends_on: ["3.4", "4.3", "5.1", "6.5"]
owns: "`src/lib.rs` orchestration types"
primary_tests: "compile/placeholder audit"
---

# Task 7.0: Define orchestration-owned types and dependency injection

## Outcome and required behavior
Define `Services { clock, github, git, process }` traits/adapters used only where deterministic testing requires them; production adapters remain thin. Define `TickOutcome` (`Processed`, `Idle304`, `IdleEmpty`, `SkippedConcurrent`, `SkippedCadence`, `RateLimited`, `Cancelled`, `Failed`), `FailureClass` (`Worker`, `Infrastructure`, `RateLimit { reset_at }`, `Cancellation`), and one exhaustive `classify_error` match. Schema/result/content/no-code-change/worker-exit/timeout failures are `Worker`; HTTP transport/server, git transport, filesystem, and teardown failures are `Infrastructure`; typed rate limits and cancellation retain their own class. New `CaduceusError` variants make this match fail to compile until classified.

Define `ActiveRunGuard`, which owns claim, optional worktree, supervisor handle/worker PGID, and cancellation state. Its async `finish_*` methods perform explicit transitions; `Drop` logs an invariant violation but synchronous drop is not relied upon for cleanup.

There is one canonical worker signature (Task 5.1), one canonical worktree type (Task 4.2), and one canonical finalization context (Task 5.0). Delete any earlier temporary signatures when enabling orchestration.

Acceptance: a repository-wide check finds no `unimplemented!`, `todo!`, placeholder ellipses, duplicate public function declarations, or `expect`/`unwrap` in production modules handling external input.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/7.0.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
