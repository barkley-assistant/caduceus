---
task_id: "0.1"
title: "Create the Rust crate and module graph"
execution_phase: 0
phase_file: "../phases/00-scaffolding.md"
depends_on: []
owns: "`Cargo.toml`, `Cargo.lock`, `src/main.rs`, `src/lib.rs`, all module stubs"
primary_tests: "compile/fmt/clippy gates"
---

# Task 0.1: Create the Rust crate and module graph

## Outcome and required behavior
Create `Cargo.toml`, committed `Cargo.lock`, `src/main.rs`, and modules `config`, `context`, `error`, `finalize`, `github`, `issue`, `logging`, `meta`, `migrate`, `poll`, `prompt`, `queue`, `status`, `validate`, `verify`, `worktree`, `worker`, and `worker_supervisor`. Add all dependencies listed above now so later tasks do not mutate the toolchain opportunistically. Re-export only `CaduceusError`, `CaduceusResult`, `IssueDetail`, `IssueKey`, queue enums/entries, and `WorkerResult` from `lib.rs`; modules otherwise use their canonical paths.

Acceptance: `cargo build --locked --all-targets`, `cargo fmt --check`, and `cargo clippy --locked --all-targets -- -D warnings` pass on the pinned MSRV. `main` parses the canonical CLI only; it does not print a version during a normal cron tick.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/0.1.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
