---
task_id: "5.2"
title: "Construct a deny-by-default worker environment"
execution_phase: 5
phase_file: "../phases/05-workers.md"
depends_on: ["1.1", "2.6", "4.2"]
owns: "`src/worker.rs`"
primary_tests: "`tests/worker_env_test.rs`"
---

# Task 5.2: Construct a deny-by-default worker environment

## Outcome and required behavior
Implement `sanitized_env(args) -> CaduceusResult<BTreeMap<OsString, OsString>>` from an injected parent environment for testability. `spawn` must call `env_clear()` before `envs`. Preserve exact default variables and configured prefix patterns, but hard-deny `GITHUB_TOKEN`, `GH_TOKEN`, `CADUCEUS_GITHUB_TOKEN`, `AUTO_ISSUE_GITHUB_TOKEN`, variables containing `GITHUB` plus `TOKEN`, and any daemon-internal secret. Emit labels as JSON and all paths as absolute UTF-8 or return an error.

Tests execute a real child that dumps its environment; cover all contract variables, empty labels, labels containing commas, denied credentials despite allowlist, approved provider keys, unrelated AWS secret removed, invalid path, and values absent from logs.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/5.2.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
