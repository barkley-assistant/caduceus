---
task_id: "2.1"
title: "Build the typed HTTP client and persistent conditional cache"
execution_phase: 2
phase_file: "../phases/02-github.md"
depends_on: ["1.2", "1.5"]
owns: "`src/github.rs`"
primary_tests: "`tests/github_client_test.rs`"
---

# Task 2.1: Build the typed HTTP client and persistent conditional cache

## Outcome and required behavior
Implement `github::Client::with_config(&Config) -> CaduceusResult<Client>` and a shared, mutex-protected `HttpCache` rooted at `<state_dir>/cache/http.json`. Configure a 10-second connect timeout and `http_timeout_seconds` total request timeout. Redirects are limited to three and only to the same scheme/host/port as `api_base`; authorization is never forwarded elsewhere. Responses retain their final URL so issue verification can detect a transfer. Centralize headers, status mapping, ETag validation, atomic cache writes, cached-body reuse on 304, and a streaming response-size limit of 10 MiB before full allocation. Cache keys are full URL plus relevant Accept header. Concurrent detail requests merge cache entries through one locked update; they never overwrite from independent stale snapshots.

Tests: required headers, auth header, connect/request timeout, allowed same-host redirect, cross-host redirect rejected without token forwarding, redirect loop, first 200 then a new client sending `If-None-Match`, 304 body reuse, cache corruption recovery, invalid ETag, chunked oversized body, 401/403/404/500 mapping, and no token in errors.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/2.1.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
