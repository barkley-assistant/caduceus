---
task_id: "3.2"
title: "Create and release atomic claims"
execution_phase: 3
phase_file: "../phases/03-state.md"
depends_on: ["3.1"]
owns: "`src/queue.rs`"
primary_tests: "`tests/claim_test.rs`, `tests/daemon_lock_test.rs`"
---

# Task 3.2: Create and release atomic claims

## Outcome and required behavior
`acquire_next` creates the SHA-256 claim path with `create_new(true)`, writes/syncs claim JSON, then marks the same entry `InProgress` while holding `state.lock`. If state persistence fails, it removes the new claim. If claim creation loses a race, it tries the next FIFO entry. `ClaimToken` contains the digest path and run ID but does not expose arbitrary deletion.

Add a separate nonblocking `DaemonLock::try_acquire(state_dir) -> CaduceusResult<Option<DaemonLock>>` held for the entire tick. Its file handle releases the lock on drop; the file itself may remain.

Tests: two threads and two subprocesses yield one claim winner, two daemon locks yield one winner, rollback after state-write failure, claim JSON durability, hostile key cannot affect paths, and completed claims are deleted.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/3.2.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
