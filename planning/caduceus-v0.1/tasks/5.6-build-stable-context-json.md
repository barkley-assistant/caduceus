---
task_id: "5.6"
title: "Build stable context JSON"
execution_phase: 5
phase_file: "../phases/05-workers.md"
depends_on: ["1.1", "2.6"]
owns: "`src/context.rs`"
primary_tests: "`tests/context_test.rs`"
---

# Task 5.6: Build stable context JSON

## Outcome and required behavior
Emit schema version, issue timeline, filtered `issue_comments`, and explicit `trusted_comments`. Parse allowlist entries during config validation. Invalid ignore regexes are configuration errors, never silently dropped. A comment is trusted by exact login or numeric ID; ignored authors are absent from both arrays. Cap each comment body at 64 KiB and the encoded context at 1 MiB. Remove oldest untrusted comments first, then oldest trusted comments only if necessary, and emit counts/byte truncation metadata; timeline events are similarly bounded before failing.

Tests: empty schema snapshot, login/id trust, rename-resistant ID, ignored bot, case-sensitive author matching with an explicit `(?i)` opt-in, invalid regex rejected in config, timeline serialization, stable chronological order, exact per-body/total boundaries, trusted-last truncation order/metadata, irreducibly oversized timeline, Unicode, and JSON round-trip.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/5.6.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
