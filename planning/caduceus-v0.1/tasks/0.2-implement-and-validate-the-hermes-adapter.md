---
task_id: "0.2"
title: "Implement and validate the Hermes adapter"
execution_phase: 1
phase_file: "../phases/01-foundations.md"
depends_on: ["0.1"]
owns: "root `plugin.yaml`, `__init__.py`, `skills/caduceus/SKILL.md`, `plugin-assets/*`"
primary_tests: "`tests/hermes_plugin_test.py`"
---

# Task 0.2: Implement and validate the Hermes adapter

## Outcome and required behavior
Replace the historical `plugin/` scaffolding with the root-level layout in the Hermes compatibility contract. First migrate the reference bridge source to `plugin-assets/worker-bridge.py`, then delete the entire legacy `plugin/` directory (`README.md`, `SKILL.md`, `commands/`, `cron/`, `bin/`, `plugin.yaml`, and the old bridge) in the same commit. The only remaining Hermes surface is the root-level canonical layout. Implement root `__init__.py` registration and its explicit setup/doctor/status/cron lifecycle. Keep the adapter stdlib-only so Hermes can discover it before the Rust binary is built. All subprocess calls use argument arrays, bounded output, timeouts, and redacted errors. The bridge template remains the only harness-specific Python file; the Hermes adapter never imports it or handles daemon credentials.

Tests run against Hermes Agent 0.18.2 in an isolated `HERMES_HOME` and prove: install from repository root; plugin discovery and enablement; manifest field allowlist; skill resolution as `caduceus:caduceus`; slash and CLI command registration; missing-binary diagnostics; locked Rust build and atomic binary placement; setup idempotency; user bridge preservation and `.new` upgrade candidate; cron wrapper path/content/mode; cron zero/one/multiple-match reconciliation; no-agent execution invokes `caduceus run`; cron removal; source update followed by rebuild; plugin removal leaves user bridge/state; registration performs no build/network/config/cron mutation; and the legacy `plugin/` directory is absent. A negative fixture containing the legacy custom manifest fields must fail the contract test.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/0.2.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
