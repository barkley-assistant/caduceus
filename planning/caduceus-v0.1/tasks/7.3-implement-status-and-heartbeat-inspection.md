---
task_id: "7.3"
title: "Implement status and heartbeat inspection"
execution_phase: 7
phase_file: "../phases/07-orchestration.md"
depends_on: ["3.1", "7.1", "7.2"]
owns: "`src/status.rs`, `src/main.rs`"
primary_tests: "`tests/status_test.rs`, golden fixtures"
---

# Task 7.3: Implement status and heartbeat inspection

## Outcome and required behavior
Rust worker supervision writes heartbeat JSON `{version,run_id,pid,started_at,updated_at,issue_key,transcript_path}` atomically every 30 seconds. `live_workers` accepts only regular non-symlink `.heartbeat` files updated within 90 seconds, parses the run ID directly from `file_stem`, validates paths, and uses saturating elapsed-time arithmetic.

`build_report(config) -> CaduceusResult<StatusReport>` uses `StateStore::snapshot`, strict metadata load, phase counts including `Previewed`, FIFO eligible queued head, earliest future eligibility when all queued entries are backed off, at most 10 recent failed/skipped queue errors plus daemon diagnostics (including ambiguous trigger labels), and live workers. Human output has golden fixtures matching README; JSON has a schema-version field. `status` loads normal config so custom state directories work.

Tests: exact README idle output, running output with transcript, JSON snapshot, all phase counts, deterministic head, missing state diagnostic, corrupt state diagnostic, fresh/stale/future/malformed/symlink heartbeat, and custom config path.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/7.3.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
