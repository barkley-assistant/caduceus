---
task_id: "6.5"
title: "Finalize failures and investigations"
execution_phase: 6
phase_file: "../phases/06-finalization-maintenance.md"
depends_on: ["5.3", "6.6"]
owns: "`src/finalize.rs`, `src/github.rs`"
primary_tests: "`tests/failure_investigation_test.rs`"
---

# Task 6.5: Finalize failures and investigations

## Outcome and required behavior
Worker/process failures update local state first. After revalidating that the issue remains open/triggered, a best-effort generic failure comment may name the run ID but never claims a local transcript is a public link; withdrawal suppresses the comment, and comment failure does not hide the original error. Investigation success copies the result to the runs directory and persists `InvestigationReady` before posting findings. Findings combine summary with the same bounded, injection-safe artifact renderer used for PR bodies. Revalidate the issue/label immediately before posting and again before label removal; withdrawal skips the remaining mutations. Otherwise post/reuse the marker comment, persist `InvestigationCommented`, remove only the configured investigation label, leave the issue open, and perform no git mutation. A retry resumes the checkpoint without invoking the worker. Both paths are idempotent and voice-checked.

Tests: failure comment, forbidden original error not leaked, comment API failure preserves worker error, investigation comment/label removal, retry marker reuse, issue remains open, and no push/PR calls.

## Execution boundaries

- Read [CONTRACTS.md](../CONTRACTS.md), the phase file, and all dependency handoffs first.
- Implement only this task. The manifest owns dependency and file-ownership metadata.
- Original monolith snippets are non-normative and must not be copied when they conflict with this packet or CONTRACTS.md.
- Do not weaken, ignore, or delete an acceptance assertion to make the task pass.
- Do not add network-dependent tests; use deterministic local fixtures and mocks.
- A required edit outside the owned files must be explained in the handoff and remain within this task's contract.

## Completion

1. Demonstrate RED for new behavior where a test can express it.
2. Make the primary tests pass.
3. Run `cargo fmt --check`, `cargo clippy --locked --all-targets -- -D warnings`, and `cargo test --locked --all-targets` once the Rust workspace exists. Python-only pre-scaffold work runs its named pytest suite and the plan validator.
4. Check the task's forbidden side effects explicitly.
5. Write `../handoffs/6.5.md` from the template, including exact commands and results.
6. Mark progress complete only after verification; then stop.
