# Handoff — Task 2.3: Unify production worker execution

- Work item: 2.3 (Unify production worker execution)
- Outcome: complete
- Files changed:
  - src/worker.rs (modified; removed `#![allow(dead_code)]`, `DENIED_SUBSTRINGS` const, `resolve_command` stub)
  - src/tick.rs (modified; added nonzero-exit rejection guard, worktree result path, archive before finalization, renamed `_result_path` → `worker_result_path`)
  - src/finalize.rs (modified; extracted `archive_worker_result` from inline `commit_code_result` copy, kept `write_atomic` for crash safety)
  - tests/worker_archive_test.rs (new; 8 acceptance tests for archive, nonzero-exit rejection, signaled bypass, no-file error)
  - tests/commit_test.rs (modified; updated `commit_writes_result_to_runs_dir` to use `archive_worker_result` before passing to finalization)
  - planning/caduceus-v1.0/handoffs/2.3.md (this file)
- Public signatures/contracts used:
  - planning/caduceus-v1.0/CONTRACTS.md RUN-001 (ONE production worker path — sanitized env, supervise, accept worktree result, archive before finalization, reject nonzero exit)
  - planning/caduceus-v1.0/tasks/2.3-unify-production-worker-execution.md (the task packet: 5 ACs, owned surfaces: worker adapter, tick, result handling)
  - planning/caduceus-v1.0/handoffs/2.1.md (precedent: config bootstrap, worker_command resolution path)
  - planning/caduceus-v1.0/handoffs/1.2.md (precedent: fixture pattern, test conventions)
  - planning/caduceus-v1.0/handoffs/1.3.md (precedent: fixture pattern, supervisor test conventions)
  - src/worker.rs (existing API: sanitized_env, spawn, parse_result_file, validate_worker_result)
  - src/worker_supervisor.rs (existing API: supervise, SupervisorOutcome, WorkerRunPaths)
  - src/finalize.rs (existing API: write_atomic, commit_code_and_finalize)
- State/schema effects: progress.json transitioned from pending → in_progress. No daemon state schema changes.
- Tests added or changed:
  - tests/worker_archive_test.rs (8 new tests: archive copies to runs/, archive enforces 0600 mode, archive creates runs/ dir if missing, archive fails on missing source, nonzero exit 1 rejects, nonzero exit 2 rejects, zero exit with no file returns error, signaled exit bypasses nonzero check)
  - tests/commit_test.rs (1 test updated: `commit_writes_result_to_runs_dir` now uses `archive_worker_result` as the tick would)
- Commands run:
  - cargo fmt --check (no diff)
  - cargo clippy --locked --all-targets -- -D warnings (0 warnings)
  - cargo test --locked --all-targets (all passed: 51 Rust test binaries, 0 failures)
  - python3 -m pytest tests/hermes_plugin_test.py tests/bridge_test.py -q (65 passed)
  - python3 -B planning/caduceus-v1.0/tools/validate_plan.py (plan valid: 42 tasks, 8 phases, acyclic and phase-safe)
  - sha256sum planning/caduceus-v1.0/CONTRACTS.md (unchanged)
  - git status planning/caduceus-v0.1/ (clean; archive untouched)
- Results: every named acceptance check below passed; the 8 new tests prove all 5 ACs with deterministic file-based checks
- Forbidden-side-effect checks:
  - v0.1 archive untouched; git status planning/caduceus-v0.1/ reports no changes
  - progress.json is only modified by set_status.py (2.3 transitioned to in_progress)
  - no production source modified outside src/worker.rs, src/tick.rs, src/finalize.rs
  - the new test file lives under tests/ which is the documented location for integration test infrastructure
  - no prompts/ or operator scratch files in the working tree
  - CONTRACTS.md is unchanged (sha256 matches); planning/caduceus-v0.1/ tree digest is unchanged
- Residual risks:
  - The nonzero-exit rejection guard runs before parse_result_file. If the worker exits non-zero but still writes a valid worker-result.json, the result is discarded. This is the contractually correct behavior (RUN-001: "reject a result when the worker exits nonzero").
  - The `archive_worker_result` function reads the full file into memory before writing. The 1 MiB cap (MAX_RESULT_FILE_BYTES) in parse_result_file bounds the maximum. The archive runs before the finalization path, so a corrupt or oversized result file is caught by parse_result_file on the archive path, not by the archive itself.
  - The DENIED_SUBSTRINGS removal was verified: the `denied_name` function in `src/worker.rs` still checks exact-name denials (GITHUB_TOKEN, GH_TOKEN, etc.) and the "contains both GITHUB and TOKEN" substring pattern — the removed `DENIED_SUBSTRINGS` const was unused because the pattern matching is done inline in `denied_name`.
  - The `resolve_command` stub was removed. The `worker_command` is resolved from the Config chain (Task 2.1's `default_worker_command`). If a future task needs a separate command resolution path, it should add it to the `Config` struct or expose it through a new module — not re-add a stub returning `Vec::new()`.
- Blocker evidence: n/a, task is complete

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 2.3-AC-01 | PASS | `cargo test --test worker_archive_test -- archive_copies_worktree_result_to_runs_dir archive_creates_runs_dir_if_missing` | archive_worker_result reads the worktree result, creates runs/ if missing, writes via write_atomic; the archived file content matches the worktree source | tests/worker_archive_test.rs lines 49-75, 104-123 |
| 2.3-AC-02 | PASS | `cargo test --test worker_archive_test -- archive_copies_worktree_result_to_runs_dir` | Result is read from `worktree.path.join("worker-result.json")` — the worktree path, not the runs directory | tests/worker_archive_test.rs lines 49-75; src/tick.rs lines 471-485 |
| 2.3-AC-03 | PASS | `cargo test --test worker_archive_test -- archive_copies_worktree_result_to_runs_dir archive_result_has_0600_mode` | archive runs before finalization (tick.rs step 16 before step 17); the archive creates runs/ with 0600 mode | src/tick.rs lines 488-497; src/finalize.rs archive_worker_result |
| 2.3-AC-04 | PASS | `cargo test --test worker_archive_test -- nonzero_exit_rejects_result_file nonzero_exit_2_also_rejects zero_exit_no_result_file_returns_error signaled_exit_bypasses_nonzero_check` | Four tests covering: exit 1 rejects, exit 2 rejects, exit 0 + no file → parse error, signaled exit bypasses the guard | tests/worker_archive_test.rs lines 152-234; src/tick.rs lines 459-469 |
| 2.3-AC-05 | PASS | `cargo test --test worker_archive_test` then grep for `#![allow(dead_code)]` in src/worker.rs, `resolve_command` in src/worker.rs, `DENIED_SUBSTRINGS` in src/worker.rs | `#![allow(dead_code)]` removed from src/worker.rs; `resolve_command` stub removed; `DENIED_SUBSTRINGS` const removed; no todo!/unimplemented! in any production path | git diff src/worker.rs; src/worker.rs current state |