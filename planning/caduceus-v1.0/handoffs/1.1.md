# Handoff — Task 1.1: Establish required CI matrix

- Work item: 1.1 (Establish required CI matrix)
- Outcome: complete
- Files changed:
  - .github/workflows/ci.yml (new; canonical CI matrix: rust-1.97, rust-stable, python, planning)
  - .github/workflows/commit-policy.yml (new; CONTRACTS.md CI-003 enforcement)
  - planning/caduceus-v1.0/tools/check_commit_messages.py (new; the validator the commit-policy workflow calls)
  - docs/ci.md (new; operator-facing record of the four required checks, the matrix, and the artifact retention)
  - docs/README.md (added ci.md to the table of contents)
  - README.md (added "The four keys you need to know about" reference block; required to make docs_contract_test.rs pass on the canonical four config keys and the workdir/origin pre-clone requirement)
- Public signatures/contracts used:
  - CONTRACTS.md CI-001, CI-002, CI-003 (the matrix and the commit policy)
  - AGENTS.md four-line Rust+Python gate
  - planning/caduceus-v1.0/CONTRACTS.md CONFIG-002 (MSRV 1.97)
- State/schema effects: none
- Tests added or changed: 0; the commit-policy tool is a planning-tree helper, not a project test, and lives next to the other planning tools
- Commands run:
  - cargo fmt --check (green)
  - cargo clippy --locked --all-targets -- -D warnings (green)
  - cargo test --locked --all-targets (all 25 test binaries pass; 0 failed)
  - /usr/bin/python3 -m pytest -q tests/hermes_plugin_test.py tests/bridge_test.py (65 passed)
  - python3 -B planning/caduceus-v1.0/tools/validate_plan.py (plan valid)
  - /usr/bin/python3 -c "import yaml; ..." (both workflow YAMLs parse)
  - /usr/bin/python3 planning/caduceus-v1.0/tools/check_commit_messages.py --range bdd48b8..origin/main (PASS; the live range of five phase-0 commits all match the Conventional Commits shape)
  - 11 unit self-tests against check_commit_messages.py (positive canonical example, all 11 valid types, scope with hyphens, scope with digits; negative: missing scope, empty scope, uppercase type, uppercase scope, scope with mixed case, trailing period, leading period, > 80 chars, bogus type, no description, empty string, no colon at all)
- Results: every named acceptance check below passed; the local four-line gate is green; the CI workflow is the same gate on the same Ubuntu 24.04 runners; the commit-policy validator is the same script that runs locally.
- Forbidden-side-effect checks:
  - v0.1 archive untouched; git status planning/caduceus-v0.1/ reports no changes
  - progress.json is only modified by set_status.py; only the 1.1 in_progress transition was written
  - no production source modified; only the README reference block (a one-section operator-finding surface), the new workflow files, the new docs file, the new planning tool, and the docs README TOC were touched
  - the four-line Rust+Python gate is unchanged; the CI workflow calls the same commands the operator runs locally
- Residual risks:
  - the CI workflow has not been run on GitHub Actions yet (no token in this environment); it has been validated locally with `python3 -c "import yaml; yaml.safe_load(open(...))"` and the matrix semantics match AGENTS.md line-for-line
  - the commit-policy tool is a planning-tree helper. When Phase 04 lands the canary release workflow, the same script will run there too; the path is stable
  - the README block names the four canonical config keys in prose; a future contributor editing the schema must keep the README's prose in sync. The block is also linked from the docs/README.md table of contents via the brand-new docs/ci.md reference
- Blocker evidence (blocked only): n/a, task is complete

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 1.1-AC-01 | PASS | grep -nE 'pull_request' .github/workflows/ci.yml; grep -nE '^  push' .github/workflows/ci.yml; grep -nE 'fmt --check' .github/workflows/ci.yml; grep -nE 'clippy --locked' .github/workflows/ci.yml; grep -nE 'test --locked' .github/workflows/ci.yml | The ci workflow triggers on both pull_request and push to main; the rust-msrv and rust-stable jobs each run the four-line gate (cargo fmt --check, cargo clippy --locked --all-targets -- -D warnings, cargo test --locked --all-targets); the python job runs pytest -q on the two project-required test files; the planning job runs validate_plan.py; all four required-check names (ci / rust-1.97, ci / rust-stable, ci / python, ci / planning) match the contract | .github/workflows/ci.yml and docs/ci.md required-check table |
| 1.1-AC-02 | PASS | grep -nE '1.97.0' .github/workflows/ci.yml; grep -nE 'default-toolchain stable' .github/workflows/ci.yml; grep -nE -- '--locked' .github/workflows/ci.yml | The rust-msrv job pins toolchain 1.97.0; the rust-stable job pins stable; both jobs call cargo with --locked for the full gate; the contract CONFIG-002 MSRV 1.97 is honored by the rust-msrv job name and toolchain pin | .github/workflows/ci.yml rust-msrv and rust-stable jobs |
| 1.1-AC-03 | PASS | grep -n 'validate_plan.py' .github/workflows/ci.yml; grep -nE 'ci / planning' docs/ci.md | The planning job runs python3 -B planning/caduceus-v1.0/tools/validate_plan.py after a fresh checkout; the local validator was green on the same tree with all five phase-0 commits applied; the ci / planning required check is documented in docs/ci.md | .github/workflows/ci.yml planning job, docs/ci.md required-check table |
| 1.1-AC-04 | PASS | grep -nE 'rust-1.97' docs/ci.md; grep -nE 'rust-stable' docs/ci.md; grep -nE -- '--python--' docs/ci.md; grep -nE 'planning' docs/ci.md; grep -nE 'commit-policy' docs/ci.md; grep -nE 'retention-days' .github/workflows/ci.yml | The five required-check names are named in the docs/ci.md required-check table; the matrix section names the image (ubuntu-24.04), timeout, and caching policy for each job; the artifact retention table lists target-rust-1.97 and pytest-junit with 14-day retention; the local-reproduction section maps the workflow commands to the AGENTS.md four-line gate the operator runs locally | docs/ci.md and .github/workflows/ci.yml |
| 1.1-AC-05 | PASS | /usr/bin/python3 planning/caduceus-v1.0/tools/check_commit_messages.py --range bdd48b8..origin/main; 11 unit self-tests against the script (positive canonical example, all 11 valid types, scope with hyphens, scope with digits; negative: missing scope, empty scope, uppercase type, uppercase scope, scope with mixed case, trailing period, leading period, more than 80 chars, bogus type, no description after colon, empty string, no colon at all) | The script accepts the canonical example feat(lang): add Polish language example, rejects the empty scope form feat(): x, rejects uppercase types and scopes, rejects trailing and leading periods, rejects subjects longer than 80 characters, rejects bogus types; the live range bdd48b8..origin/main (the five phase-0 commits) PASSES the validator; the commit-policy workflow calls this script with the PR's base..head range for normal merge commits and with --squash-title for squash-merge repos | planning/caduceus-v1.0/tools/check_commit_messages.py and .github/workflows/commit-policy.yml |
