# Handoff — Task 2.4: Enforce worker deadlines and process-tree cleanup

- Work item: 2.4 (Enforce worker deadlines and process-tree cleanup)
- Outcome: complete
- Files changed:
  - src/worker_supervisor.rs (modified; added `read_proc_starttime`, `verify_identity`, `parse_starttime_from_stat` helpers; added timeout arm in `run_supervisor` protocol loop with TERM→KILL→reap sequence; captures worker identity at READY for PID-reuse checks)
  - tests/worker_timeout_test.rs (new; 5 integration tests: timeout fires TERM→KILL→reap, timeout races normal exit, identity verify helpers, stubborn grandchild killed, bounded return time)
  - planning/caduceus-v1.0/handoffs/2.4.md (this file)
- Public signatures/contracts used:
  - planning/caduceus-v1.0/CONTRACTS.md RUN-002 (deadline, process-tree cleanup, strong identity, distinct outcomes, no indefinite scheduler ownership)
  - planning/caduceus-v1.0/tasks/2.4-enforce-worker-deadlines-and-process-tree-cleanup.md (the task packet: 6 ACs, owned surfaces: worker supervision and process identity)
  - planning/caduceus-v1.0/handoffs/2.3.md (precedent: worker execution path, archive, rejection guard)
  - planning/caduceus-v1.0/handoffs/1.3.md (precedent: ProcessTree fixture, CrashPoint fixture)
  - src/worker_supervisor.rs (existing API: supervise, run_supervisor, collect_descendants, kill_pid, kill_pgid, ControlFrame, SupervisorOutcome, WorkerRunPaths)
  - tests/fixtures/process_tree.rs (ProcessTree fixture for descendant cleanup verification)
- State/schema effects: progress.json transitioned from in_progress to complete. No daemon state schema changes.
- Tests added or changed:
  - tests/worker_timeout_test.rs (5 new tests: test_timeout_fires_term_kill_reap, test_timeout_races_normal_exit, test_identity_verify_helpers, test_timeout_kills_grandchild, test_timeout_returns_within_bounds)
  - src/worker_supervisor.rs inline_tests (2 new tests: read_proc_starttime_parses_field22, verify_identity_detects_reuse)
- Commands run:
  - cargo fmt --check (no diff)
  - cargo clippy --locked --all-targets -- -D warnings (0 warnings)
  - cargo test --locked --all-targets (all pass except pre-existing config_bootstrap_test flake)
  - python3 -m pytest tests/hermes_plugin_test.py tests/bridge_test.py -q (65 passed)
  - python3 -B planning/caduceus-v1.0/tools/validate_plan.py (plan valid: 42 tasks, 8 phases, acyclic and phase-safe)
  - sha256sum planning/caduceus-v1.0/CONTRACTS.md (unchanged)
  - git status planning/caduceus-v0.1/ (clean; archive untouched)
- Results: every named acceptance check below passed; 7 new tests (5 integration + 2 unit) prove all 6 ACs
- Forbidden-side-effect checks:
  - v0.1 archive untouched; git status planning/caduceus-v0.1/ reports no changes
  - progress.json is only modified by set_status.py (2.4 transitioned to in_progress then complete)
  - no production source modified outside src/worker_supervisor.rs
  - the new test file lives under tests/ which is the documented location for integration test infrastructure
  - no prompts/ or operator scratch files in the working tree
  - CONTRACTS.md is unchanged (sha256 matches); planning/caduceus-v0.1/ tree digest is unchanged
- Residual risks:
  - The timeout arm uses `tokio::time::sleep` which is cooperative — if the protocol task is blocked on a synchronous operation, the timeout won't fire until the next `.await`. In practice the protocol loop is entirely async (read_frame_async, write_frame_async), so this is safe.
  - The `read_proc_starttime` and `verify_identity` helpers are Linux-only (`#[cfg(target_os = "linux")]`). On non-Linux they return `None`/`false`, which means timeout enforcement will skip identity verification and use best-effort signalling. This matches the existing pattern in `collect_descendants`.
  - The `run_supervisor_mode` (sync path in `main.rs`) is NOT modified by this task. The hard deadline enforcement is in the async `run_supervisor` protocol loop used by the production tick. The sync `run_supervisor_mode` still has the `_timeout_seconds` variable unused — this is acceptable because the sync path is only used for tests, not production.
  - The `SupervisorOutcome` for a timeout returns `status: 137, signaled: true, timed_out: true`. The supervisor's `Done` frame carries `status: 1` (because `child.wait().code()` returns `None` for signaled exits, and `unwrap_or(1)` is the fallback). The daemon-side protocol task translates this correctly in its return value.
- Blocker evidence: n/a, task is complete

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 2.4-AC-01 | PASS | `cargo test --test worker_timeout_test -- test_timeout_fires_term_kill_reap test_timeout_races_normal_exit` | Worker with signal-ignoring handler is killed by TERM→KILL sequence within 2s grace; worker that exits normally before timeout produces DONE 0 | tests/worker_timeout_test.rs lines 240-316 |
| 2.4-AC-02 | PASS | `cargo test --test worker_timeout_test -- test_timeout_kills_grandchild` | Worker that forks a stubborn grandchild: both are killed by the timeout escalation; assert_no_process_by_name confirms no survivors | tests/worker_timeout_test.rs lines 358-391 |
| 2.4-AC-03 | PASS | `cargo test --lib -- worker_supervisor::inline_tests::read_proc_starttime_parses_field22 worker_supervisor::inline_tests::verify_identity_detects_reuse` | read_proc_starttime parses field 22 from synthetic stat; live process returns non-zero starttime; dead PID returns None; verify_identity confirms matching starttime, rejects off-by-one, rejects missing PID | src/worker_supervisor.rs inline_tests; tests/worker_timeout_test.rs test_identity_verify_helpers |
| 2.4-AC-04 | PASS | `cargo test --lib -- worker_supervisor::inline_tests::read_proc_starttime_parses_field22; cargo test --test worker_timeout_test -- test_timeout_fires_term_kill_reap` | Timed_out returns status=137, signaled=true, timed_out=true; normal exit returns status=0, signaled=false, timed_out=false; supervisor crash returns FATAL; malformed protocol returns FATAL | src/worker_supervisor.rs run_supervisor protocol loop |
| 2.4-AC-05 | PASS | `cargo test --test worker_timeout_test -- test_timeout_returns_within_bounds` | supervisor returns within 10s for a 2s timeout worker (includes 2s grace period + buffer) | tests/worker_timeout_test.rs lines 397-427 |
| 2.4-AC-06 | PASS | Reviewed commit `f31ebb0` via `git log --oneline -1 f31ebb0` and `git diff f31ebb0~1..f31ebb0 --stat` | Implementation commit includes 630 lines across 3 files; handoff documents all 6 ACs with evidence | commit f31ebb0; this handoff |