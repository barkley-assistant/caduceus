# Handoff — Task 1.2: Build hermetic GitHub and Git fixtures

- Work item: 1.2 (Build hermetic GitHub and Git fixtures)
- Outcome: complete
- Files changed:
  - tests/fixtures/mod.rs (new; re-exports LocalOrigin, MockGitHub, Counts; cfg(test)-gated)
  - tests/fixtures/github.rs (new; MockGitHub Wiremock-backed builder with CountingResponder, mount, mount_status, mount_paged, mount_etag, mount_not_modified, mount_with escape hatch, counts, received_requests, path_counts)
  - tests/fixtures/git_origin.rs (new; LocalOrigin disposable bare-repo helper with init, clone_into, push_commit, receive_push, head_oid, commit_count, run_in_bare)
  - tests/fixtures_self_test.rs (new; nine self-tests covering the three ACs)
  - tests/github_client_test.rs (migrated; MockServer replaced by MockGitHub where the convenience methods fit; 15 tests, all green)
  - tests/issue_poll_test.rs (migrated; MockServer replaced by MockGitHub; added one multiple-repos test; 13 tests, all green)
  - tests/pr_test.rs (migrated; client_for takes MockGitHub; 8 tests, all green)
  - tests/rate_limit_test.rs (migrated; mock_client takes MockGitHub; 18 tests, all green)
- Public signatures/contracts used:
  - planning/caduceus-v1.0/CONTRACTS.md CI-002 (reusable Wiremock GitHub, disposable local Git origin, real release-binary, crash-point, process-tree fixtures; hermetic; no production credentials)
  - planning/caduceus-v1.0/CONTRACTS.md CI-003 (commit-message policy enforced by the planning validator; every commit below follows the Conventional Commits shape with a non-empty scope)
  - planning/caduceus-v1.0/tasks/1.2-build-hermetic-github-and-git-fixtures.md (the task packet: owned surfaces Wiremock GitHub and disposable-origin support; primary tests are fixture self-tests)
  - planning/caduceus-v1.0/tasks/1.3-build-process-crash-release-binary-fixtures.md (precedent on how Task 1.2 integrates with later Phase 1 work)
  - planning/caduceus-v1.0/tasks/1.4-build-the-pinned-hermes-host-fixture.md (paired Phase 1 fixture; uses the same #[path = "fixtures/mod.rs"] convention)
  - src/github.rs (typed GitHub HTTP client; production code under test; wiremock and dev-dependency entry point)
  - src/worktree.rs (existing daemon-owned worktree helpers; future Phase 2.6 consumers will exercise them through LocalOrigin)
  - tests/worktree_create_test.rs (existing inline init_bare_repo helper; the precedent LocalOrigin was lifted out of)
- State/schema effects: none on the production daemon; tests/fixtures/ adds four dev-only files under tests/ which Cargo does not auto-discover as binaries; tests/fixtures_self_test.rs is the new self-test binary
- Tests added or changed: tests/fixtures_self_test.rs (9 new tests); tests/github_client_test.rs (15 tests, migrated, no count change); tests/issue_poll_test.rs (13 tests, migrated, one new multiple-repos test added); tests/pr_test.rs (8 tests, migrated, no count change); tests/rate_limit_test.rs (18 tests, migrated, no count change). Net new tests in the suite: ten (nine self-tests plus one multiple-repos)
- Commands run:
  - cargo fmt (no diff)
  - cargo fmt --check (green)
  - cargo clippy --locked --all-targets -- -D warnings (green; the file-level dead_code allow on tests/fixtures/github.rs and tests/fixtures/git_origin.rs is documented inline and reflects the per-binary compilation view Cargo uses)
  - cargo test --locked --all-targets (green: 757 passed, 0 failed, 0 ignored across all 38 test binaries)
  - cargo test --test fixtures_self_test (green: 9 passed in 0.07s; three ACs each have three self-tests)
  - cargo test --test github_client_test (green: 15 passed in 5.01s)
  - cargo test --test issue_poll_test (green: 13 passed in 0.01s)
  - cargo test --test pr_test (green: 8 passed in 0.01s)
  - cargo test --test rate_limit_test (green: 18 passed in 0.00s)
  - python3 -B planning/caduceus-v1.0/tools/validate_plan.py (plan valid: 42 tasks, 8 phases, acyclic and phase-safe)
  - python3 -B planning/caduceus-v1.0/tools/next_task.py --format json (returns Task 1.3 — Build process, crash, release-binary fixtures — as the next eligible work item)
  - python3 -B planning/caduceus-v1.0/tools/check_commit_messages.py --range bdd48b8..origin/main (PASS: the five Phase 0 plus three Phase 1 commits all match the Conventional Commits shape)
  - /usr/bin/python3 -m pytest -q tests/hermes_plugin_test.py tests/bridge_test.py (65 passed in 1.02s)
  - sha256sum planning/caduceus-v1.0/CONTRACTS.md (unchanged from the manifest-pinned contracts_sha256)
  - git status planning/caduceus-v0.1/ (clean; archive untouched)
- Results: every named acceptance check below passed; the four migrated test files exercise the new helpers on real Phase 2 production paths (typed HTTP client, labeled issue poll, PR find-or-create, rate-limit parsing); the nine self-tests prove the fixture's own contract (loopback only; file:// only; mutation counts exact)
- Forbidden-side-effect checks:
  - v0.1 archive untouched; git status planning/caduceus-v0.1/ reports no changes
  - progress.json is only modified by set_status.py; only the 1.2 in_progress transition was written this session
  - no production source modified; src/ and Cargo.toml are clean
  - the new fixtures live under tests/ which is the documented location for integration test infrastructure; tests/fixtures/ subdirectory files are referenced via #[path] from each consumer, so they are not auto-built as binaries
  - no /prompts/ or operator scratch files in the working tree; .gitignore would catch them anyway
  - CONTRACTS.md is unchanged (sha256 matches); v01_tree_sha256 is unchanged; planning/caduceus-v0.1/ tree digest is unchanged
- Residual risks:
  - tests/fixtures/github.rs and tests/fixtures/git_origin.rs carry file-level #![allow(dead_code)]; the rationale (per-binary compilation view) is documented inline at the top of each file. A future contributor who adds a helper should remove the allow only after every helper is reachable from every consuming binary
  - MockGitHub::mount_paged's first-page matcher uses query_param_is_missing("page"); a caller that pages with page=0 would miss the first mount. The contract comment names this; if Phase 2 ever pages from zero, the helper needs a first-page-also-explicit variant
  - MockGitHub::mount_with is an escape hatch that does not contribute to counts() or received_requests(); this is documented in the rustdoc but a future caller who wraps counts around it will be off by N. Phase 2.6 and 7.5 should prefer the convenience methods and reach for mount_with only when no convenience method fits
  - LocalOrigin::push_commit requires a global git identity on the test host; the helper passes -c user.name and -c user.email overrides so a fresh checkout works without setting GIT_AUTHOR_NAME. A test that wants to read the commit author must read the result via git log on the working clone, not assume an environment-derived identity
  - the migrated tests still bypass MockGitHub's convenience methods for cases that need custom wiremock matchers (headers, query_param_is_missing, NoHeader inversion). The pattern is documented in each migrated test's header comment; the wiremock escape hatch via gh.server() is the canonical path for those
- Blocker evidence (blocked only): n/a, task is complete

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 1.2-AC-01 | PASS | cargo test --test fixtures_self_test -- ac01_mock_github_uri_is_loopback ac01_local_origin_uri_is_file_scheme ac01_fixtures_run_with_no_github_token_in_environment | All three AC-01 self-tests green in 0.07s; MockGitHub.uri() always starts with http://127.0.0.1: or http://[::1]: and never contains github.com; LocalOrigin.uri() always starts with file:// and the bare repo's HEAD file exists on disk; the no-token test runs reqwest.get against the mock with no GITHUB_TOKEN, GH_TOKEN, or CADUCEUS_GITHUB_TOKEN set and the mock answers 200 | tests/fixtures_self_test.rs AC-01 tests, lines 39-99 |
| 1.2-AC-02 | PASS | cargo test --test fixtures_self_test -- ac02_push_commit_moves_origin_head_and_bumps_count ac02_push_to_missing_ref_panics_with_stderr ac02_clone_into_existing_dir_fails_cleanly | All three AC-02 self-tests green in 0.05s; push_commit bumps head_oid and commit_count by exactly one; push from detached HEAD surfaces an informative stderr message; clone into an occupied directory exits non-zero and does not delete the pre-existing file | tests/fixtures_self_test.rs AC-02 tests, lines 109-181 |
| 1.2-AC-03 | PASS | cargo test --test fixtures_self_test -- ac03_counts_track_exact_mutation_total ac03_path_counts_are_keyed_per_endpoint ac03_received_requests_preserve_order_and_method | All three AC-03 self-tests green in 0.07s; counts().post == 2, patch == 1, get == 1, mutations() == 3, total() == 4 after 4 requests via reqwest; path_counts returns HashMap with one entry per endpoint keyed by the request path; received_requests preserves the GET-then-POST order wiremock saw | tests/fixtures_self_test.rs AC-03 tests, lines 192-318 |