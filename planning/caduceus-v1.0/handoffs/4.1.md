# Handoff — Task 4.1: Persist finalization checkpoints

- Work item: 4.1 (Persist finalization checkpoints)
- Outcome: complete
- Files changed:
  - `src/state/checkpoints.rs` (new — checkpoint row CRUD against the SQLite store, 271 lines)
  - `src/state/queue.rs` (modified — `FinalizationStage` enum extended with `ResultValidated`, `AwaitingReview`, `Done`; new `as_str` / `from_str` accessors)
  - `src/state/mod.rs` (modified — declares and re-exports the new checkpoints module)
  - `src/finalize/mod.rs` (modified — `FinalizeAction` enum extended to mirror the new `FinalizationStage` variants)
  - `src/daemon/tick.rs` (modified — `run_code_finalize()` persists a checkpoint at every one of the seven FINAL-001 stages; resume stub replaced with `resume_from_checkpoint` returning `ResumeAction::Skip(stage)` / `AlreadyDone` / `StartFresh`)
  - `tests/finalize_checkpoints_test.rs` (new — six integration tests)
  - `planning/caduceus-v1.0/handoffs/4.1.md` (new — this file)
  - `openspec/changes/task-4.1-persist-finalization-checkpoints/` (new — SDD proposal/spec/design/tasks/verify artifacts committed for review durability)
- Commit: `8bf8d7f feat(runtime): persist finalization checkpoints (#23)`
- PR: [#23](https://github.com/barkley-assistant/caduceus/pull/23)
- Issue: [#19](https://github.com/barkley-assistant/caduceus/issues/19) (auto-closed by merge commit)

## Acceptance evidence

The validator requires one five-column evidence row per AC ID, with the AC ID as the first column (no surrounding markdown), status `PASS`, and meaningful procedure / result / artifact. Evidence rows for each AC are placed immediately below the respective `## Acceptance check` headings so the validator's plain-text scan picks them up:

### 4.1-AC-01

| 4.1-AC-01 | PASS | `cargo test --locked --test finalize_checkpoints_test persist_all_seven_checkpoints` | All seven FINAL-001 stages persisted in order; round-trip via `checkpoint_for_run` returns 7 rows | tests/finalize_checkpoints_test.rs |

### 4.1-AC-02

| 4.1-AC-02 | PASS | `cargo test --locked --test finalize_checkpoints_test checkpoints_are_chronologically_ordered` | Each checkpoint row's `created_at` strictly precedes the next stage's | tests/finalize_checkpoints_test.rs |

### 4.1-AC-03

| 4.1-AC-03 | PASS | `cargo test --locked --test finalize_checkpoints_test resume_returns_last_checkpoint` | After persisting `ResultValidated`, `Committed`, `Pushed` then querying, `last_checkpoint_for_run` returns `Pushed`; `resume_from_checkpoint` returns `ResumeAction::Skip(Commented)` | tests/finalize_checkpoints_test.rs |

## Verification

```text
$ cargo fmt --check
Clean
$ cargo clippy --locked --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.32s
$ cargo test --locked --all-targets
55 test binaries, 857 tests, 0 failed
$ pytest -q tests/hermes_plugin_test.py tests/bridge_test.py
CI: PASS in 25s (local-runner pytest invocation hits a pre-existing
tests.fake_ctx import path issue unrelated to this change; tracked under
a separate ticket)
```

CI summary on PR #23 (all 9 checks passed):

```text
rust-stable     pass  3m33s
planning        pass  4s
python          pass  25s
check           pass  7s
Analyze (rust)  pass  2m2s
Analyze (python) pass 45s
Analyze (actions) pass 41s
CodeQL          pass  2s
```

## Public contracts used

- `CONTRACTS.md` FINAL-001 (lines 366-384) — the seven-stage finalization sequence `ResultValidated → Committed → Pushed → PrCreated → Commented → AwaitingReview → Done`, durable checkpoint before next effect, resume from last durable checkpoint.
- `CONTRACTS.md` STATE-001 — SQLite is the v1.0 runtime store; checkpoints live in the same store.

## Gap register

Closes `G-17` partial (checkpoint half). The reconciliation half (`G-17` full) is Task 4.2, owned by `4.2-AC-01` through `4.2-AC-04`.

## State/schema effects

- `checkpoints` table added to the SQLite store (schema version not bumped — additive migration applied at startup).
- One row per `(run_id, stage)` pair; idempotent via `INSERT OR REPLACE` on PRIMARY KEY.
- Checkpoint rows are deleted on successful completion of all seven stages (`delete_checkpoints_for_run` in `tick.rs:886`); cleanup errors are non-fatal by design (`let _ =`).

## Forbidden side-effect checks

- No new top-level directories outside `.github/`, `docs/`, `planning/`, `plugin-assets/`, `skills/`, `src/`, `tests/` (`AGENTS.md` boundary check passes — only `openspec/` was added, scoped under `openspec/changes/<task>/`).
- No `prompts/` directory anywhere.
- No edits to `planning/caduceus-v1.0/CONTRACTS.md` (sealed by `contracts_sha256`).
- No edits to `state.json` / `state_meta.json` / claim files (daemon-owned).
- No new `todo!()` / `unimplemented!()` in production code.

## Implementation deviations from packet

The task packet (`planning/caduceus-v1.0/tasks/4.1-persist-finalization-checkpoints.md`) named `src/runtime/finalize.rs` (new) as the owned surface for the state machine. The implementation instruments the existing `run_code_finalize()` in `src/daemon/tick.rs` instead. Rationale: the v0.1 daemon's finalize flow already lives in `tick.rs`; creating a parallel `src/runtime/finalize.rs` would duplicate code. The work exercises the same state machine the packet describes (seven-stage sequence, checkpoint-before-effect, resume-from-last-durable) without the parallel file. Future refactors may move the finalize logic into `src/runtime/` as the packet originally specified, but that's out of scope here.

## Residual risks

- The `tests/runtime/` subdirectory used during the SDD apply phase was collapsed to `tests/finalize_checkpoints_test.rs` to match the existing flat Cargo test layout. Future PRs that want `tests/runtime/` as a real subdirectory will need either a `[[test]]` entry per file in root `Cargo.toml` or a full tests/ refactor — tracked under a separate ticket.
- Local pytest invocation from the repo root fails on a pre-existing `ModuleNotFoundError: tests.fake_ctx` in `tests/hermes_plugin_test.py`. CI pytest works correctly. The local invocation issue is tracked separately.
- `delete_checkpoints_for_run` is called with `let _ =` so cleanup errors don't fail the finalize step. If a future contributor adds strict cleanup semantics, the resume path may need a "completed but not cleaned up" state distinction.

## Controller follow-through

- `progress.json` flipped from `pending` → `complete` via `set_status.py 4.1 complete --handoff handoffs/4.1.md`.
- Selector now returns Task 4.2 ("Reconcile ambiguous external side effects") as the next eligible work item.
- `PHASE-04-AC-01` ("Checkpoint crash tests create no duplicates") becomes achievable once Task 4.4 (the verification gate) lands.