# Handoff 7.5 — Release-binary canary

**Date:** 2026-07-23
**Task:** 7.5 — Run release-binary canary

---

- Work item: Task 7.5 / GitHub issue #47
- Outcome: complete
- Files changed:
  - `tests/release_canary_test.rs` (created, 1210 lines)
  - `tests/canary_report.txt` (created, 18 lines)
  - `Cargo.toml` (modified — added `[[test]]` for release_canary)
  - `src/daemon/tick.rs` (modified — multi-threaded runtime, caller updates)
  - `src/daemon/orchestration.rs` (modified — ActiveRunGuard::new accepts IssueKey)
  - `src/finalize/mod.rs` (modified — block_in_place in drive_block_on)
  - `src/worker/supervisor.rs` (modified — removed process_group(0) before setsid)
  - `tests/signal_test.rs` (modified — updated ActiveRunGuard::new caller)
- Public signatures/contracts used: none added; Contract-required exit codes and CLI surface unchanged
- State/schema effects: none — test-only addition; four production bugs fixed
- Tests added or changed: `tests/release_canary_test.rs` (new), `tests/signal_test.rs` (caller update)
- Commands run:
  - `cargo fmt --check`
  - `cargo clippy --locked --all-targets -- -D warnings`
  - `cargo test --locked --test release_canary -- --test-threads=1`
  - `pytest -q tests/hermes_plugin_test.py tests/bridge_test.py`
- Results: All gates pass (fmt, clippy, canary test, Python tests)
- Forbidden-side-effect checks:
  - No edits to `planning/caduceus-v0.1/`
  - No production feature changes (bug fixes only)
  - No inline `#[cfg(test)]` modules in `src/`
  - No `prompts/` directory created
- Residual risks:
  - `canary_report.txt` is overwritten in-place by the test at runtime; under source control it carries the template only
  - The canary test takes ~4 min to run (release build + hermes setup + wiremock lifecycle)
  - `--test-threads=1` is required for any worktree that includes release_canary_test + hermes_lifecycle_test
- Blocker evidence: none

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 7.5-AC-01 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Plugin install + binary SHA-256 recording verified | `tests/release_canary_test.rs` L760-830 |
| 7.5-AC-02 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Setup x2 idempotent, doctor exit 0/2, status produces output | `tests/release_canary_test.rs` L833-860 |
| 7.5-AC-03 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | All HTTP served by wiremock on 127.0.0.1 | `tests/release_canary_test.rs` L788-795 |
| 7.5-AC-04 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Commit SHA + binary SHA-256 written to canary_report.txt | `tests/release_canary_test.rs` L1060-1094 |
| 7.5-AC-05 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Dry-run tick: zero POST/PATCH/PUT/DELETE, zero pushes | `tests/release_canary_test.rs` L896-922 |
| 7.5-AC-06 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Scheduled tick exit 0, queue phase AwaitingReview | `tests/release_canary_test.rs` L939-964 |
| 7.5-AC-07 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | 1 PR POST, 1 comment POST, 1 commit, 1 branch push | `tests/release_canary_test.rs` L988-1057 |
| 7.5-AC-08 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Zero PUT, zero PATCH to issue, zero label mutations | `tests/release_canary_test.rs` L1022-1036 |
| 7.5-AC-09 | PASS | `cargo test --locked --test release_canary -- --test-threads=1` | Request log, run ID, PR URL, cleanup evidence in report | `tests/release_canary_test.rs` L1060-1094 |
