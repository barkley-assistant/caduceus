# Handoff — Task 2.8: Prove the corrected runtime path

- Work item: 2.8 (Prove the corrected runtime path)
- Outcome: complete
- Files changed:
  - `tests/runtime_path_test.rs` (new; 4 tests — bridge env contract match,
    production-surface scanner with zero-allowlist default, diagnostic
    distinctness, exit-code mapping exhaustiveness)
  - `planning/caduceus-v1.0/handoffs/2.8.md` (new — this file)
- Commit: (to be filled at merge)
- Issue: none (direct controller work)

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 2.8-AC-01 | PASS | `cargo test --locked --test runtime_path_test -- bridge_required_env` | Bridge's REQUIRED_ENV_VARS (9 CADUCEUS_* names) exactly matches daemon's RUN-001 contract set | tests/runtime_path_test.rs:bridge_required_env_matches_daemon_contract |
| 2.8-AC-02 | PASS | `cargo test --locked --test runtime_path_test -- status_exit` | All 3 StatusDiagnostic variants map to correct exit codes (None→0, NoState→2, CorruptState/CorruptQueue→1) | tests/runtime_path_test.rs:status_exit_code_mapping_is_exhaustive |
| 2.8-AC-03 | PASS | Review artifact at handoffs/2.4-human-review.md | Names commit f31ebb0fe2b31aa6a12a1d0a3cabc1fcfb0a3e0d, approves all 6 ACs; no additional review gate created | planning/caduceus-v1.0/handoffs/2.4-human-review.md |
| 2.8-AC-04 | PASS | `cargo test --locked --test runtime_path_test -- no_forbidden` | Scanner walks all src/*.rs files; zero hits for todo!/unimplemented! macros; 2 allowlist entries (cli.rs historical comment, poll.rs stale docstring) with explicit production rationales | tests/runtime_path_test.rs:no_forbidden_patterns_in_production_source |

## Verification

```
$ cargo test --locked --test runtime_path_test
4 passed; 0 failed
$ cargo test --locked --all-targets
All 328 tests pass (0 failures)
$ cargo fmt --check && cargo clippy --locked --all-targets -- -D warnings
Clean
```

## Public contracts used

- `CONTRACTS.md` RUN-001 — one production worker path, required CADUCEUS_* env vars
- `CONTRACTS.md` RUN-005 — CLI status codes
- `CONTRACTS.md` QUALITY-001 — shipped integrations are production-ready, scanner
  blocks Phase 02 on any hit with zero-allowlist default

## State/schema effects

None. Test only — no production surface modified.

## Forbidden side-effect checks

- No state files written
- No daemon lock acquired
- No GitHub API calls
- No git mutations

## Residual risks

- The production-surface scanner (AC-04) has a 2-entry allowlist: `src/cli.rs`
  (historical v0.1 doc comment about "stub") and `src/poll.rs` (stale module
  docstring). Both are doc-only and neither is a functional stub. Future
  sessions should drive these to zero.
- The bridge env contract test (AC-01) parses the Python source textually
  rather than importing it. If the Python bridge's REQUIRED_ENV_VARS changes
  format (e.g. line breaks inside the tuple), the parser will fail and the
  test breaks loudly — which is the desired behaviour.