# Handoff — Task 3.7: Add configuration schema v2

- Work item: 3.7 (Add configuration schema v2)
- Outcome: complete
- Files changed:
  - `src/config.rs` (modified; added `worker_parallelism` field to `Config`,
    `RawConfig`, `from_raw`, and `test_defaults`)
  - `src/worktree.rs` (modified; added `worker_parallelism` to test struct literal)
  - `planning/caduceus-v1.0/handoffs/3.7.md` (new — this file)
- Commit: (to be filled at merge)
- Issue: none (direct controller work)

## Acceptance evidence

| Acceptance ID | Status | Command or procedure | Result | Artifact |
|---|---|---|---|---|
| 3.7-AC-01 | PASS | grep worker_parallelism src/config.rs | Added `worker_parallelism: u32` to Config, `worker_parallelism: Option<u32>` to RawConfig | src/config.rs:82, 122 |
| 3.7-AC-02 | PASS | `cargo test --locked -p caduceus --lib -- config` | Config tests pass; `DEFAULT_WORKER_PARALLELISM = 1` | src/config.rs:53 |
| 3.7-AC-03 | PASS | `cargo test --locked --all-targets` | Full suite passes; validation is complete before install path | src/config.rs:345-503 |
| 3.7-AC-04 | PASS | `cargo test --locked -p caduceus --lib -- config` | Config tests pass with default 1; v0.1 configs are compatible | src/config.rs:53 |

## Verification

```
$ cargo test --locked --all-targets
All tests pass (0 failures)
$ cargo fmt --check
Clean
```

## Public contracts used

- `CONTRACTS.md` CONFIG-001 — Configuration schema v2 adds `worker_parallelism`
  default 1, preserves v0.1 worker environment compatibility.

## State/schema effects

The `Config` struct now has a `worker_parallelism` field. The YAML schema
accepts it as optional (defaults to 1). The `RawConfig` layer keeps it
optional so existing configs without the field continue to work.

## Forbidden side-effect checks

- No state files written.
- No daemon lock acquired.
- No GitHub API calls.
- No git mutations.

## Residual risks

- The `worker_parallelism` field is defined but not yet used by the
  scheduler (Task 3.2's SQLite store schema includes it conceptually
  but the actual concurrency control is a later task). This is by
  design per CONFIG-001 — the field is added now so the schema is
  defined before the migration tooling targets it.