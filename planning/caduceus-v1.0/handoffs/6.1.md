# Handoff — Task 6.1: Introduce executor interface

- Work item: 6.1 (Introduce executor interface)
- Outcome: complete
- Commit range: `4584456..822fae8` (3 commits, 887 insertions, 26 deletions, 34 files changed)
- Files changed:
  - `src/executor/mod.rs` (new) — Executor trait, ExecutorSpec, ExecutorKind enum, executor_for_config factory, re-exports
  - `src/executor/trusted_host.rs` (new) — TrustedHostExecutor struct wrapping worker::supervisor::supervise
  - `src/executor/oci.rs` (new) — OciExecutor stub returning OciNotImplementedYet
  - `src/lib.rs` (modified) — Added `pub mod executor;` + bounded re-exports
  - `src/infra/error.rs` (modified) — Added OciNotImplementedYet and ReducedContainmentNotAcknowledged variants, Debug arms
  - `src/infra/config.rs` (modified) — Added executor_mode and reduced_containment_acknowledged fields, from_raw validation, test_defaults update
  - `src/infra/fixtures.rs` (modified) — Added executor_mode and reduced_containment_acknowledged to CANONICAL_CONFIG_KEYS
  - `src/daemon/orchestration.rs` (modified) — Services.executor field, production/for_tests constructors, executor_for_config factory, 2 classify_error arms, exhaustive array 28→30
  - `src/daemon/tick.rs` (modified) — Replaced services.process.supervise(...) with services.executor.run(&spec).await
  - `src/worktree/mod.rs` (modified) — MinimalConfig test_defaults update
  - `Cargo.toml` (modified) — 4 new [[test]] entries with edition = "2021"
  - `tests/executor_trusted_host_test.rs` (new) — 4 tests: dyn dispatch, object-safety, parity, subprocess grep
  - `tests/executor_oci_test.rs` (new) — 2 tests: not-implemented, no-spawn
  - `tests/executor_config_test.rs` (new) — 8 tests: opt-in, load, defaults, round-trip, Oci config
  - `tests/tick_dispatch_test.rs` (new) — 1 test: grep-based dispatch assertion
  - 14 existing test files updated with reduced_containment_acknowledged: Some(true) or YAML equivalent
- Public signatures/contracts used:
  - `Executor` trait (object-safe, fn run(&self, &ExecutorSpec) -> Pin<Box<dyn Future>>)
  - `ExecutorSpec` struct (7 fields: self_exe, issue, worktree, run_id, context_json, worker_command, cancellation)
  - `ExecutorKind` enum (TrustedHost | Oci, serde rename_all = "snake_case", default TrustedHost)
  - `executor_for_config(&Config) -> Arc<dyn Executor>` factory
  - `TrustedHostExecutor::new(cfg) -> Self` and `TrustedHostExecutor::run` delegating to supervise
  - `OciExecutor::new(cfg) -> Self` and `OciExecutor::run` returning OciNotImplementedYet
  - `CaduceusError::OciNotImplementedYet` — display contains "Task 6.2"
  - `CaduceusError::ReducedContainmentNotAcknowledged` — display mentions opt-in
  - `Config::executor_mode: ExecutorKind` (default TrustedHost)
  - `Config::reduced_containment_acknowledged: bool` (default false)
  - `Services.executor: Arc<dyn Executor>` field
  - Dispatch site: `services.executor.run(&spec).await` in tick.rs:581
  - CONTRACTS.md clauses: EXEC-001, EXEC-002 (new executor contract lines)
- State/schema effects: None. No SQLite migration, no state file format change. Config fields are `Option<T>` in RawConfig — backward-compatible for existing YAML files. However, existing YAML without `reduced_containment_acknowledged: true` will fail `from_raw` with TrustedHost mode (default). Operators must add the field.
- Tests added or changed:
  - `tests/executor_trusted_host_test.rs` — 4 tests (new)
  - `tests/executor_oci_test.rs` — 2 tests (new)
  - `tests/executor_config_test.rs` — 8 tests (new)
  - `tests/tick_dispatch_test.rs` — 1 test (new)
  - 14 existing test files — 1 line each (reduced_containment_acknowledged: Some(true))
  - Total: 15 new tests, 0 regressions
- Commands run:
  - `TMPDIR=/home/agent/.tmp cargo fmt --check` — exit 0, clean
  - `TMPDIR=/home/agent/.tmp cargo build --locked --all-targets` — exit 0, passes (only pre-existing [[test]] edition deprecation warnings)
  - `TMPDIR=/home/agent/.tmp cargo test --locked --all-targets` — 1030 passed, 0 failed, exit 0
  - `TMPDIR=/home/agent/.tmp cargo test --locked --test executor_trusted_host_test` — 4 passed, 0 failed
  - `TMPDIR=/home/agent/.tmp cargo test --locked --test executor_oci_test` — 2 passed, 0 failed
  - `TMPDIR=/home/agent/.tmp cargo test --locked --test executor_config_test` — 8 passed, 0 failed
  - `TMPDIR=/home/agent/.tmp cargo test --locked --test tick_dispatch_test` — 1 passed, 0 failed
  - `TMPDIR=/home/agent/.tmp pytest -q tests/hermes_plugin_test.py tests/bridge_test.py` — 105 passed, exit 0
  - `python3 -B planning/caduceus-v1.0/tools/validate_plan.py` — plan valid, exit 0
- Results: 1030/1030 Rust tests pass, 105/105 Python tests pass. 0 pre-existing failures, 0 new failures, 0 warnings.
- Forbidden-side-effect checks:
  - `git log --format='%H %an <%ae>' -3` — 3 commits by `Hermes Agent <barkleyassistant@gmail.com>` (operator-mandated identity)
  - `git diff --stat HEAD~3..HEAD -- planning/caduceus-v1.0/CONTRACTS.md` — empty (sealed contract untouched)
  - `git diff --stat HEAD~3..HEAD -- state.json state_meta.json` — empty (daemon-owned state untouched)
  - `ls -d prompts/ 2>/dev/null` — nothing (no banned operator-prompt directory)
  - `grep -RE 'ghp_|github_pat_' src/` — zero hits outside the scrub step (PAT hygiene preserved)
  - `grep -RE 'tokio::process::Command' src/executor/` — 0 hits (subprocess owned by trusted_host.rs only)
  - `grep -nE 'services\.process\.supervise' src/daemon/tick.rs` — 0 hits (dispatch fully migrated)
  - `grep -nE 'services\.executor\.run' src/daemon/tick.rs` — 1 hit (the trait-object dispatch at line 581)
- Residual risks:
  - `ProcessSupervisor` and `ProcessSupervisorAdapter` retained in `Services` as transitional. Task 6.2 removes them. Code that uses `services.process` directly (non-tick paths) still works but creates dead field risk until 6.2.
  - `Config::from_raw` now rejects TrustedHost mode without `reduced_containment_acknowledged: true`. Existing YAML files without this field will fail to load. Operators must add the field. `test_defaults` sets it to `true` — all existing test configs that use `test_defaults` continue to work.
  - `OciExecutor::run` returns `OciNotImplementedYet` immediately. This is the expected stub behavior for Task 6.2 to fill in. Deployment with `executor_mode: oci` will reject all worker dispatches at runtime.
  - The `cfg` clone in `TrustedHostExecutor::new(cfg.clone())` and `OciExecutor::new(cfg.clone())` duplicates the Config in memory. Config is < 1KB, so this is negligible.
  - Local pytest without `PYTHONPATH=.` fails with `ModuleNotFoundError: tests.fake_ctx`. CI sets the path correctly. This is a pre-existing local-tooling quirk, not a regression.

## Acceptance evidence

### 6.1-AC-01 — Route workers through the executor interface

| 6.1-AC-01 | PASS | `cargo test --locked --test executor_trusted_host_test --test tick_dispatch_test` | 4 tests + 1 test pass. Trait is object-safe (`Arc<dyn Executor>` compiles in Services). tick.rs calls `services.executor.run(&spec).await` (line 581). Legacy `services.process.supervise(...)` removed from tick.rs. `tokio::process::Command` appears only in `src/worker/supervisor.rs` (unchanged), not in `src/executor/`. | tests/executor_trusted_host_test.rs, tests/tick_dispatch_test.rs |

### 6.1-AC-02 — Keep trusted-host execution compatible and explicit

| 6.1-AC-02 | PASS | `cargo test --locked --test executor_trusted_host_test --test executor_config_test` | Parity test passes: TrustedHostExecutor::run matches supervisor::supervise for identical inputs. YAML round-trip works. Config defaults to TrustedHost. test_defaults loads with opt-in. | tests/executor_trusted_host_test.rs, tests/executor_config_test.rs |

### 6.1-AC-03 — Require reduced-containment acknowledgement

| 6.1-AC-03 | PASS | `cargo test --locked --test executor_config_test --test executor_oci_test` | Config::from_raw rejects TrustedHost + unacknowledged (error contains "reduced_containment_acknowledged"). Oci config loads cleanly. OciExecutor::run returns OciNotImplementedYet with "Task 6.2" in message. No subprocess is spawned during config validation. | tests/executor_config_test.rs, tests/executor_oci_test.rs |

## Verification

```text
$ TMPDIR=/home/agent/.tmp cargo fmt --check
Clean
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo build --locked --all-targets
warning: `edition` is set on test ... (pre-existing deprecation warnings)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.24s
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo test --locked --test executor_config_test
8 passed; 0 failed
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo test --locked --test executor_oci_test
2 passed; 0 failed
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo test --locked --test executor_trusted_host_test
4 passed; 0 failed
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo test --locked --test tick_dispatch_test
1 passed; 0 failed
Exit: 0

$ TMPDIR=/home/agent/.tmp cargo test --locked --all-targets
1030 passed; 0 failed
Exit: 0

$ TMPDIR=/home/agent/.tmp pytest -q tests/hermes_plugin_test.py tests/bridge_test.py
105 passed in 2.42s
Exit: 0

$ python3 -B planning/caduceus-v1.0/tools/validate_plan.py
plan valid (active catalog): 42 tasks, 8 phases, acyclic and phase-safe
Exit: 0
```

## Commits and delivery

| Commit | Description |
|--------|-------------|
| `4584456` | feat(executor): add executor module with trait and impls |
| `3fe6044` | feat(infra): add executor_mode and reduced_containment_acknowledged to Config |
| `822fae8` | refactor(daemon): route tick dispatch through executor trait object |

Delivery mode: single PR with `size:exception` (operator pre-approved). 887 lines added, 26 removed across 34 files. 3 conventional commits.
