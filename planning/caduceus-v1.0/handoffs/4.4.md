# Handoff — Task 4.4: Verify checkpoint and lifecycle recovery

- Work item: 4.4 (Verify checkpoint and lifecycle recovery)
- Outcome: complete
- Files changed:
  - `tests/checkpoint_crash_matrix_test.rs` (new — 22 `#[test]` cases covering FINAL-001 crash-recovery idempotency: 1 empty-run baseline plus 7 stages × 3 crash points (before-checkpoint, between-checkpoint-and-effect, after-effect))
  - `tests/lifecycle_matrix_test.rs` (new — 5 `#[test]` cases covering FINAL-002 lifecycle transitions: `AwaitingReview → Done` on merge, `AwaitingReview → NeedsAttention` on close-without-merge, reopen-then-merge, reprocess refusal of `AwaitingReview`, and `refuse_auto_merge` error path)
  - `tests/credentials_audit_test.rs` (new — 3 `#[test]` cases covering FIN-001 credential hygiene: known-pattern match, safe-string non-match, and a fixture-scanner proof that the Phase 04 test corpus leaks no credentials)
  - `planning/caduceus-v1.0/handoffs/phase-04.md` (new — the Phase 04 gate handoff, authored by the orchestrator and aggregating this task plus Tasks 4.1–4.3)
- Public signatures/contracts used:
  - `CONTRACTS.md` FINAL-001 (lines 366–384) — durable checkpoint state machine (`ResultValidated → Committed → Pushed → PrCreated → Commented → AwaitingReview → Done`) used as the matrix of crash stages
  - `CONTRACTS.md` FINAL-002 (lines 385–392) — human merge lifecycle (merge → Done, close-without-merge → NeedsAttention, daemon MUST NOT auto-merge)
  - `CONTRACTS.md` FIN-001 — credential leakage prohibition: no `ghp_*`, `github_pat_*`, `x-access-token`, or basic-auth headers in public text, test fixtures, or evidence files
- State/schema effects: none. This task is pure verification — no production code changes. The schema v2 columns (`operation_id`, `remote_marker`) introduced by Task 4.2 are exercised read-only by the crash matrix.
- Tests added or changed:
  - `tests/checkpoint_crash_matrix_test.rs` — new (22 tests)
  - `tests/lifecycle_matrix_test.rs` — new (5 tests)
  - `tests/credentials_audit_test.rs` — new (3 tests)
  - No production source files modified; no existing tests changed.
- Commands run:
  - `cargo fmt --check` — clean
  - `cargo clippy --locked --all-targets -- -D warnings` — clean (0 warnings)
  - `cargo test --locked --all-targets` — 901 passed, 0 failed across 60 test binaries
  - `cargo test --locked --test checkpoint_crash_matrix_test` — 22 passed, 0 failed
  - `cargo test --locked --test lifecycle_matrix_test` — 5 passed, 0 failed
  - `cargo test --locked --test credentials_audit_test` — 3 passed, 0 failed
  - `PYTHONPATH=. pytest -q tests/hermes_plugin_test.py tests/bridge_test.py` — 105 passed
- Results: 22 + 5 + 3 = 30 new tests, all green. Full Rust suite delta +30 (871 → 901) with zero regressions. Pytest delta unchanged. No `unsafe`, `todo!`, or `unimplemented!` introduced. No edits to `state.json`, `state_meta.json`, claim files, or `CONTRACTS.md`.
- Forbidden-side-effect checks:
  - `git log --since='2026-07-20T16:00Z' --format='%H %an'` confirms four new commits are authored by the project-standard `Hermes Agent <barkleyassistant@gmail.com>` agent identity (matches the direct-to-main commits on Tasks 4.1, 4.2, 4.3)
  - `git diff --stat 0719f78..HEAD -- planning/caduceus-v1.0/CONTRACTS.md` is empty — the sealed contract was not edited
  - `git diff --stat 0719f78..HEAD -- state.json state_meta.json` is empty — daemon-owned state was not edited in place
  - `ls -d prompts/ 2>/dev/null` returns nothing — no banned operator-prompt directory was created in the repo
  - The credentials audit fixture-scanner proof confirms zero `ghp_*`, `github_pat_*`, `x-access-token`, or basic-auth patterns appear in any captured stdout/stderr/log artifact of the Phase 04 test corpus.
- Residual risks:
  - The crash-injection tests rely on the `std::sync::Barrier` pattern documented in Task 1.2's fixtures. Slow CI runners that drift past the barrier wait window may race the SQLite commit; mitigation is to follow the Task 1.2 precedent (`barrier.wait()` before each crash point) which is already in place.
  - The credentials audit uses a four-pattern literal list plus the fixture scanner. Encoded or obfuscated credentials (e.g. base64-wrapped tokens) are out of scope for AC-03; a future phase may upgrade to regex-based detection.
  - The reopen-then-merge lifecycle test re-seeds the state file directly rather than driving the full daemon pipeline. End-to-end realism could be added in Phase 07 (full-system verification) if needed.
  - Local pytest without `PYTHONPATH=.` fails with `ModuleNotFoundError: tests.fake_ctx`. CI sets the path correctly; this is a pre-existing local-tooling quirk, not a regression from this task.

## Acceptance evidence

The validator requires one five-column evidence row per AC ID, with the AC ID as the first column (no surrounding markdown), status `PASS`, and meaningful procedure / result / artifact. Evidence rows for each AC are placed immediately below the respective `## Acceptance check` headings so the validator's plain-text scan picks them up:

### 4.4-AC-01

| 4.4-AC-01 | PASS | `cargo test --locked --test checkpoint_crash_matrix_test` | 22 tests cover every FINAL-001 stage at three crash points (before-checkpoint, between-checkpoint-and-effect, after-effect); resume in each case lands on the correct stage with no duplicate checkpoint rows and no double effects | tests/checkpoint_crash_matrix_test.rs |

### 4.4-AC-02

| 4.4-AC-02 | PASS | `cargo test --locked --test lifecycle_matrix_test` | 5 tests cover the FINAL-002 reachable paths through AwaitingReview to Done on merge, AwaitingReview to NeedsAttention on close-without-merge, reopen-then-merge completion, reprocess refusal of AwaitingReview, and the refuse_auto_merge error path | tests/lifecycle_matrix_test.rs |

### 4.4-AC-03

| 4.4-AC-03 | PASS | `cargo test --locked --test credentials_audit_test` | 3 tests confirm known credential patterns are recognized, safe strings are not flagged, and the scanner finds no occurrences of ghp_, github_pat_, x-access-token, or basic-auth forms in the Phase 04 fixture corpus | tests/credentials_audit_test.rs |

## Verification

```text
$ cargo fmt --check
Clean

$ cargo clippy --locked --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.28s
0 warnings

$ cargo test --locked --test checkpoint_crash_matrix_test
22 passed; 0 failed

$ cargo test --locked --test lifecycle_matrix_test
5 passed; 0 failed

$ cargo test --locked --test credentials_audit_test
3 passed; 0 failed

$ cargo test --locked --all-targets
901 passed; 0 failed

$ PYTHONPATH=. pytest -q tests/hermes_plugin_test.py tests/bridge_test.py
105 passed in 2.23s
```

## Commits and delivery

| Commit | Description |
|---|---|
| a17c91c | `test(checkpoint): add 22-test crash matrix for FINAL-001 checkpoint recovery` |
| 7528a44 | `test(lifecycle): add 5-test lifecycle matrix for FINAL-002 human review` |
| 5079f1f | `test(audit): add credentials audit scanner for 4.4-AC-03` |
| 90e8148 | `docs(handoff): add Phase 04 gate handoff for checkpoint and lifecycle` (Closes #22) |

Delivery mode: direct-to-main push (mirrors Task 4.2's pattern, as this issue was scoped as a pure verification task with no production code surface and no human-review requirement). Issue #22 is auto-closed by the merge commit's `Closes #22` trailer.
