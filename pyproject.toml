[project]
name = "caduceus"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]

[dependency-groups]
test = [
    "pytest>=9.0.2",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]