schema: spec-driven
skip_specs: true
created: 2026-08-22
goal: |
  Fix worktree reconcile failure on macOS (#60) caused by /var/folders
  symlink mismatch in git_worktree_list() path comparisons. Normalize both sides
  (map keys + probe paths) so idempotent replay with the same run id reconciles
  instead of hitting the foreign-collision guard. Scoped to
  src/worktree/worktree.rs. Verify the full Rust test suite remains green on
  Linux.